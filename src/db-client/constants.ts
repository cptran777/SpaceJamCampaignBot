import path = require("path");

export const ASSIGNED_COMMANDS_DB = path.join(process.cwd(), "db/tables/assigned-commands.json");
export const ASSIGNED_SUB_COMMANDS_DB = path.join(process.cwd(), "db/tables/assigned-subcommands.json");
export const CAMPAIGNS_DB = path.join(process.cwd(), "db/tables/campaigns.json");
